import React from 'react'
import Navbar from '../Navbar';
import SubNavbar from '../SubNavbar';

const User = (props) => {
    console.log(props);
  return (
    <div>
      <Navbar/>
      <SubNavbar/>
    </div>
  )
}

export default User
